--- a/include/download.mk
+++ b/include/download.mk
@@ -245,6 +245,7 @@ define DownloadMethod/rawgit
 	echo "Packing checkout..." && \
 	$(call dl_tar_pack,$(TMP_DIR)/dl/$(FILE),$(SUBDIR)) && \
 	mv $(TMP_DIR)/dl/$(FILE) $(DL_DIR)/ && \
+	sleep 2 && \
 	rm -rf $(SUBDIR);
 endef
 
